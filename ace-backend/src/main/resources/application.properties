spring.application.name=ace

# === MariaDB datasource ===
spring.datasource.url=jdbc:mariadb://localhost:3306/ace_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=ace_user
spring.datasource.password=rhzrjpz,hfrhzryenfz
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# === Hibernate / JPA ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# === Logging ===
logging.level.org.springframework=INFO
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# === Spring Security (dev only) ===
spring.security.user.name=admin
spring.security.user.password=admin
